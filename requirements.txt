# Alpha-Scanner (Signal & Research Hub) — requirements.txt
# NOTE: Intentionally excludes SmartApi / pyotp / broker SDKs.
# This module never logs in to Angel One and never places orders.

streamlit==1.38.0
pandas==2.2.2
numpy==1.26.4
yfinance==0.2.43
ta==0.11.0
supabase==2.7.4
tenacity==8.5.0
pytz==2024.1
python-dotenv==1.0.1
plotly==5.23.0
